# AtlasX — 切勿提交密钥
.env
*.env.local
.DS_Store
build-src/
*.pem
*.key
# 绝不把 PAT / 凭据放进仓
secrets/
.gh-token
.github_token
# 发版机私有：构建 Dockerfile 不进公开安装仓
Dockerfile
Dockerfile.*
docker-entrypoint.sh
docker-compose.mvp.yml
docker-compose.from-clean.yml
